[project]
name = "techrium-python-tools"
version = "0.1.0"
description = "Utility scripts for tech-atrium-wiki"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
